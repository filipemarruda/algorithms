``` javascript

/**
 * Você deve escrever um algoritmo que recebe uma string de tamanho N e retorne o seu primeiro 
 * caractere que se repete. Caso a string não possua nenhum caractere repetido, o algoritmo deve
 * retornar uma string vazia.
 * 
 * Restrições:
 *  - 1 <= N <= 10^5
 *  - Os recursos de memória e processamento do computador limitam o programa a uma ordem de 
 *    10 milhões de iterações. 
 *  - Considere que o padrão de encoding dos caracteres da string é UTF-32 (2^32 caracteres ~ 10^9).
 * 
 * Exemplo:
 *  - Entrada: "BCDEACXEK"
 *  - Saída: "C"
 * 
 * **/

/**
 * Ian - Dica 
 * 
 * n log n 
 * 
 * 10 ^ 5 
 * 
 * **/


```

// Problem solving
// Communication
// Verification
// Coding


a photographer, 
an actor, 
a backdrop

Such a photograph is considered artistic 
if the distance between the photographer and the actor is between XX and YY cells (inclusive),
and the distance between the actor and the backdrop is also between XX and YY cells (inclusive).
